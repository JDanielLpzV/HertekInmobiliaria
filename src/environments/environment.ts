// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  firebaseConfig: {
    apiKey: "AIzaSyB53VLb2Fr8CNQlrNea0zZc4-d56cS2uTk",
    authDomain: "inmobiliaria-529be.firebaseapp.com",
    databaseURL: "https://inmobiliaria-529be.firebaseio.com",
    projectId: "inmobiliaria-529be",
    storageBucket: "inmobiliaria-529be.appspot.com",
    messagingSenderId: "94661879844",
    appId: "1:94661879844:web:d822db7d85300be40790b1",
    measurementId: "G-NH99XQ03JB"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
